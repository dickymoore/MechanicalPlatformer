Replace `</path/to/chromedriver>` with the path to chromedriver and `<CloudFront_Website_URL>` with the actual CloudFront URL. Don't forget to set up an index.html in your s3 bucket with the content as per your message.